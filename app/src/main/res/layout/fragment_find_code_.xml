<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fragFindCode">

    <FrameLayout
        android:id="@+id/framePicture"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </FrameLayout>

    <RelativeLayout
        android:gravity="center"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:padding="5dp">

        <EditText
            android:id="@+id/editFindCode"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_centerVertical="true"
            android:background="@drawable/boder"
            android:hint="@string/hint_findcode"
            android:imeOptions="actionSearch"
            android:maxLines="1"
            android:inputType="text"
            android:drawableLeft="@drawable/qr_code24">

        </EditText>
        <ImageView
            android:id="@+id/imgSearchCode"
            android:padding="5dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/editFindCode"
            android:layout_alignBottom="@id/editFindCode"
            android:layout_alignRight="@id/editFindCode"
            android:focusable="true"
            android:src="@drawable/searchcode" />

</RelativeLayout>


</FrameLayout>